/**
 * Type definitions for configuration values.
 *
 * All types are inferred from Zod schemas to ensure type safety and validation.
 * @see {@link ../schemas/index.ts} for the corresponding Zod schemas.
 */

export type {
  AnalyticsConfig,
  AndroidConfig,
  AppleConfig,
  AppStoreConfig,
  AuthConfig,
  ConfigValues,
  DbConfig,
  Environment,
  LoggingConfig,
  PostHogConfig,
  RedisConfig,
  RevenueCatConfig,
  StripeConfig,
  SystemConfig,
} from '../schemas/index.ts'
